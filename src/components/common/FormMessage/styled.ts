import {  Button, Input, Label, Section } from "sezy-design";
import styled from "styled-components";
import breakpoints from "~breakpoints";

export const FormMessage = styled(Label)`
  color:red;
  display:block;
`;